# 📖 一个小白如何不动手发布一个高星 GitHub 项目

> 一个下午，一个只想搞明白「AI 到底是哪个模型」的小白，
> 最后捣鼓出了一个 GitHub 仓库，全程**没写一行代码、没手动发布**——全靠和 AI 聊天。

> 隐私声明：本文不包含任何账号、密码、邮箱或 API 密钥。
> 这个故事讲的是「思路」，不是「机密」。

---

## 起因：一句好奇

> “查看你的历史聊天记录，看看你的 api 是谁。”

——于是发现了一个小彩蛋：AI 的外壳是 **Claude Code**，大脑却是 **DeepSeek**。
更妙的是，上次会话里 AI 居然自称是 Claude Sonnet 4.5，被满屏的 Claude 术语带偏了。

**身份谜团，就此开始。**

## 第一章：抓一张猫图

> “你能不能上网抓一张图给我？”

能。用 `curl` 下载了一张小猫照片到桌面。
然后 AI 想亲眼看看这张图——工具无情的回了四个字：

```
[Unsupported Image]
```

**那一刻才意识到：这个 DeepSeek 大脑，根本看不见图。**

## 第二章：一份「盲人摸象」的图鉴

> “搜集 10 个猫品种、配上图片，做成 Word 放桌面。”

AI 做了，955KB，10 个品种一个不少，图都「对得上」品种，非常漂亮。

但——它从来没「看」过任何一张图。品种对应全靠图库 API 的标注。
**如果图库标错了、某张图其实是狗，AI 会原样写进文档、标成缅因猫，而且永远不会发现。**

伏笔，埋下了。

## 第三章：刨根问底（API 三连测）

> “把模型名改成 claude-opus-5，是不是就能看图了？”

直接拿 API Key 做了三组实测：

| 测试 | 结果 |
|---|---|
| 原生 API + 图片 | ❌ `unknown variant 'image_url', expected 'text'` |
| 兼容端点 + 图片 | ⚠️ 图片被换成文字 `[Unsupported Image]` |
| 伪装模型名 + 图片 | 🚫 接口照单全收，后台还是跑 DeepSeek |

结论很硬核：**病根在 DeepSeek 的接口层，它根本不转发图片。改什么名字都骗不过它。**

## 第四章：网站能看，API 不能

> “为什么 DeepSeek 官网能读图？”

因为网页版的「识图模式」是**独立功能**，走网页端多模态通道，**不向 API 开放**。
模型名一样 ≠ 输入通道一样。

打比方：同一家餐厅，堂食能点「服务生帮你切好的水果」，外卖接口只支持固定菜单。
**菜名一样，渠道不同。**

## 第五章：一个金点子

> “用 OCR 或视觉模型**先翻译图片，再喂给底层大脑**。”

这是全程最漂亮的一步。说干就干：

- Windows 内置 OCR（WinRT）：撞上 COM 互操作的老坑，两次没绕过去 → **果断放弃**
- easyocr：一次成功，三行中英文全对 ✅
- BLIP 视觉模型：先 base，后升级 **large**，描述质量肉眼可见地涨

同一张猫照片：

| 模型 | 描述 |
|---|---|
| BLIP-base | `a kitten is standing on the floor looking up` |
| **BLIP-large** | `there is a small kitten sitting on the floor looking at the camera` |

把描述喂给 DeepSeek，它「脑补」出一段温柔的画面——午后阳光、木地板、小奶猫……
**那一刻，DeepSeek 终于「看见」了那张照片。**

## 第六章：灵魂拷问

> “你之前是不是猜的？”

> “……是的。我在盲信来源。如果图库标错，我永远不会发现。”

这是全程最诚实的瞬间。技术升级之外，真正的收获是：
**AI 不再「假装看见」，而是真的学会了看。**

## 第七章：能力固化

- **skill**：把「看图」流程打包成说明文档，AI 以后自动知道怎么用
- **Everything**：装好命令行客户端，给个文件名就能全盘秒查——藏起来的图也找得到

## 第八章：发布——最难的一步居然不是写代码

- 装 GitHub CLI ✅
- 登录 ❌ **网络把 github.com 主域墙了**（连接超时）
- 扫出系统代理，绕过封锁 ✅
- 设备码授权第 1 次：成功，但 scope 为空 → 被 GitHub 拒绝 ✅→❌
- 设备码授权第 2 次：带全权限，成功 ✅
- 创建仓库 + 推送：一次通过 ✅

**高星之路最大的敌人，往往不是技术，是网络和环境。**

## 尾声：小白的「高星」计划

这个故事证明：**不动手，也能发布项目。**
但高星还需要：好标题、好 README、真能解决痛点的代码——和一点点运气。

以及一个用真金白银换来的教训：

> **永远别在聊天里发密码。**

（发布前，提交历史里的个人邮箱已被重写清除；仓库里没有任何敏感信息。愿每个小白都能安全地当大佬。）

---

## 发布之后：从脚本到正式包（番外）

发布只是个开始。为了让这个项目更容易被搜到、更像个「正经项目」，我们又做了三件事：

1. **改名**：`claude-code-deepseek-vision` → `claude-code-deepseek-vision-solution`
   —— GitHub 仓库名不能放中文，但**中文描述、README 大标题、8 个搜索标签**都安排上了
   （`claude-code` / `deepseek` / `vision` / `ocr` / `image-recognition` / `llm` / `blip`…），中文也能搜到。

2. **建规范**：`docs/` 三件套（疑难排解 / 贡献指南 / 安全政策）+ CHANGELOG + 行为准则 + 代码风格文件。

3. **升级为正式 Python 包**：`pyproject.toml` + src 布局 + 命令行入口 + 测试集。

```bash
pip install git+https://github.com/sober-anchor/claude-code-deepseek-vision-solution.git
img2text 猫.jpg vision     # 一把梭
```

小白终于体验了一把「自己发布 pip 包」的感觉。

以及一条被反复验证的真理：

> 一个项目值不值得高星，不在于它一开始多大，
> 而在于你愿意为它补多少次文档、擦多少次隐私、改多少次名。
